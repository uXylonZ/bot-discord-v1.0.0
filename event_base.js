const Discord = require("discord.js")
const { QuickDB } = require('quick.db');
const db = new QuickDB();
const config = require("../config.json")
require("colors")

module.exports = {
    name: 'aaaaaaaa',
    async execute(client, interaction, commands, member) {
        
        // asdasd

    }
}
